# Alembic configuration for the Kairos project.
#
# Deliberately minimal: the database URL is NOT stored here. It is resolved
# at runtime by migrations/env.py via kairos.config.database_url(), so the
# same configuration works across environments (env var KAIROS_DATA_DIR).

[alembic]
script_location = migrations
